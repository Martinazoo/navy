export interface User {
  id: string;
  name: string;
  surname: string;
  email: string;
  bachelor_degree: string;
}

export interface UserPositionResponse {
  id: string;
  posX: number;
  posY: number;
}